if(sessionStorage.getItem('delong_admin')!=='1'){location.href='login.html';throw new Error('auth');}
(()=>{const U=Delong.Utils,id=U.q('id');let db=Delong.Store.load(),b=db.bookings.find(x=>x.id===id);if(!b){location.href='bookings.html';return}function render(){db=Delong.Store.load();b=db.bookings.find(x=>x.id===id);const r=db.rooms.find(x=>x.id===b.roomId),bal=U.balance(b);const pay=(b.payments||[]).map(p=>`<tr><td>${U.fmtDateTime(p.date)}</td><td>${U.escape(p.method)}</td><td>${U.money(p.amount)}</td><td>${U.escape(p.note||'')}</td></tr>`).join('')||'<tr><td colspan="4" class="empty">Chưa có thanh toán</td></tr>';const actions=['Chờ xác nhận','Chờ cọc','Đã xác nhận','Đang ở','Đã trả phòng','Đã hủy'].map(s=>`<button class="btn btn-secondary btn-sm statusBtn" data-status="${s}">${s}</button>`).join('');const content=`<div class="admin-page-head"><div><h1>${U.escape(b.id)} · ${U.escape(b.customerName)}</h1><div class="muted">${U.escape(r.name)} · ${U.fmtDateTime(b.checkin)} → ${U.fmtDateTime(b.checkout)}</div></div>${U.statusBadge(b.status)}</div><div class="grid" style="grid-template-columns:1.1fr .9fr"><div class="card card-pad"><h3>Thông tin booking</h3><div class="form-grid"><div><div class="small muted">Khách</div><strong>${U.escape(b.customerName)}</strong></div><div><div class="small muted">Điện thoại</div><strong>${U.escape(b.phone)}</strong></div><div><div class="small muted">Số khách</div><strong>${b.guests||'-'}</strong></div><div><div class="small muted">Nguồn</div><strong>${U.escape(b.source)}</strong></div><div><div class="small muted">Hình thức</div><strong>${U.escape(b.method)}</strong></div><div><div class="small muted">Nhân viên</div><strong>${U.escape(b.staff)}</strong></div><div class="field full"><div class="small muted">Ghi chú</div><div>${U.escape(b.note||'—')}</div></div></div><h3 style="margin-top:22px">Đổi trạng thái</h3><div class="toolbar">${actions}</div></div><div class="card card-pad"><h3>Thanh toán</h3><div class="kpi-grid" style="grid-template-columns:1fr 1fr"><div class="kpi"><div class="kpi-label">Tổng</div><div class="kpi-value" style="font-size:21px">${U.money(b.total)}</div></div><div class="kpi"><div class="kpi-label">Còn lại</div><div class="kpi-value" style="font-size:21px">${U.money(bal)}</div></div></div>${bal>0?`<form id="paymentForm" style="margin-top:16px"><div class="form-grid"><div class="field"><label>Số tiền</label><input name="amount" type="number" value="${bal}" min="1" required></div><div class="field"><label>Hình thức</label><select name="method"><option>Chuyển khoản</option><option>Tiền mặt</option></select></div><div class="field full"><label>Ghi chú</label><input name="note" placeholder="Cọc / thanh toán..." ></div></div><button class="btn btn-primary" style="margin-top:10px">Ghi nhận thu tiền</button></form>`:''}</div></div><div class="card card-pad" style="margin-top:18px"><h3>Lịch sử thanh toán</h3><div class="table-wrap"><table class="table"><thead><tr><th>Thời gian</th><th>Hình thức</th><th>Số tiền</th><th>Ghi chú</th></tr></thead><tbody>${pay}</tbody></table></div></div>`;Delong.AdminLayout.mount('bookings',content,'Chi tiết booking');document.querySelectorAll('.statusBtn').forEach(btn=>btn.onclick=()=>{Delong.Store.update(x=>Delong.BookingService.setStatus(x,id,btn.dataset.status));render()});const f=document.getElementById('paymentForm');if(f)f.onsubmit=e=>{e.preventDefault();const fd=new FormData(f);Delong.Store.update(x=>Delong.BookingService.addPayment(x,id,{amount:Number(fd.get('amount')),method:fd.get('method'),note:fd.get('note')}));render()}}render();})();
