if(sessionStorage.getItem('delong_admin')!=='1'){location.href='login.html';throw new Error('auth');}
(()=>{const U=Delong.Utils,id=U.q('id'),db=Delong.Store.load(),r=db.rooms.find(x=>x.id===id);if(!r){location.href='rooms.html';return}const content=`<div class="admin-page-head"><div><h1>Sửa ${U.escape(r.name)}</h1><div class="muted">Mỗi phòng có cấu hình độc lập</div></div><a class="btn btn-secondary" href="rooms.html">← Danh sách phòng</a></div><div class="card card-pad"><form id="roomForm"><div class="form-grid"><div class="field"><label>Tên phòng</label><input name="name" value="${U.escape(r.name)}"></div><div class="field"><label>Số khách tối đa</label><input name="maxGuests" type="number" min="1" value="${r.maxGuests}"></div><div class="field"><label>Diện tích m²</label><input name="area" type="number" value="${r.area}"></div><div class="field"><label>Giường</label><input name="bed" value="${U.escape(r.bed)}"></div><div class="field full"><label>Mô tả</label><textarea name="description">${U.escape(r.description)}</textarea></div><div class="field full"><label>Tiện nghi (cách nhau bằng dấu phẩy)</label><input name="amenities" value="${U.escape(r.amenities.join(', '))}"></div>${r.slots.map((s,i)=>`<div class="field"><label>${s.label} ${s.start}–${s.end}</label><input name="price${i}" type="number" value="${s.price}"></div>`).join('')}</div><button class="btn btn-primary" style="margin-top:14px">Lưu phòng</button></form></div>`;Delong.AdminLayout.mount('rooms',content,'Sửa phòng');document.getElementById('roomForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target);Delong.Store.update(db=>{let x=db.rooms.find(x=>x.id===id);x.name=f.get('name');x.maxGuests=Number(f.get('maxGuests'));x.area=Number(f.get('area'));x.bed=f.get('bed');x.description=f.get('description');x.amenities=f.get('amenities').split(',').map(x=>x.trim()).filter(Boolean);x.slots.forEach((s,i)=>s.price=Number(f.get('price'+i)))});U.toast('Đã lưu thay đổi')}})();
