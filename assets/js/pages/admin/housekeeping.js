if(sessionStorage.getItem('delong_admin')!=='1'){location.href='login.html';throw new Error('auth');}
(()=>{const U=Delong.Utils;function render(){const db=Delong.Store.load();const content=`<div class="admin-page-head"><div><h1>Dọn phòng</h1><div class="muted">Bẩn → Đang dọn → Đã dọn</div></div></div><div class="grid">${db.rooms.map(r=>`<div class="card room-admin-card"><img src="../${r.images[0]}"><div><strong>${U.escape(r.name)}</strong><div class="small muted">Trạng thái hiện tại: ${U.escape(r.status)}</div></div><div class="actions toolbar"><button class="btn btn-danger btn-sm st" data-id="${r.id}" data-st="Bẩn">Bẩn</button><button class="btn btn-secondary btn-sm st" data-id="${r.id}" data-st="Đang dọn">Đang dọn</button><button class="btn btn-primary btn-sm st" data-id="${r.id}" data-st="Đã dọn">Đã dọn</button></div></div>`).join('')}</div>`;Delong.AdminLayout.mount('housekeeping',content,'Dọn phòng');document.querySelectorAll('.st').forEach(x=>x.onclick=()=>{Delong.Store.update(db=>{db.rooms.find(r=>r.id===x.dataset.id).status=x.dataset.st});render()})}render();})();
