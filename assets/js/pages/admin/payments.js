if(sessionStorage.getItem('delong_admin')!=='1'){location.href='login.html';throw new Error('auth');}
(()=>{const db=Delong.Store.load(),U=Delong.Utils;let pays=[];db.bookings.forEach(b=>(b.payments||[]).forEach(p=>pays.push({...p,bookingId:b.id,customerName:b.customerName,roomId:b.roomId})));pays.sort((a,b)=>b.date.localeCompare(a.date));const rows=pays.map(p=>`<tr><td>${U.fmtDateTime(p.date)}</td><td><a href="booking-detail.html?id=${p.bookingId}">${U.escape(p.bookingId)}</a></td><td>${U.escape(p.customerName)}</td><td>${U.escape(db.rooms.find(r=>r.id===p.roomId)?.name||'')}</td><td>${U.escape(p.method)}</td><td><strong>${U.money(p.amount)}</strong></td><td>${U.escape(p.note||'')}</td></tr>`).join('');Delong.AdminLayout.mount('payments',`<div class="admin-page-head"><div><h1>Thanh toán</h1><div class="muted">Mỗi booking có thể có nhiều lần thu tiền</div></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Thời gian</th><th>Booking</th><th>Khách</th><th>Phòng</th><th>Hình thức</th><th>Số tiền</th><th>Ghi chú</th></tr></thead><tbody>${rows||'<tr><td colspan="7" class="empty">Chưa có thanh toán</td></tr>'}</tbody></table></div>`,'Thanh toán');})();
